Runner sprite assets are stored in this folder so Unity Resources.Load can resolve them at runtime.

Anubis: idle, walk, run, jump, slide.
Ra: cloud idle, cloud cast, orb.

Textures are imported as Sprite (2D), Point filtered, no mipmaps, no compression.
